import React from 'react';

const Headerpart = () => {
    return (
        <header>
            <h1>Truck Tracker</h1>
            {/* Add navigation links or logo here */}
        </header>
    );
};

export default Headerpart;
